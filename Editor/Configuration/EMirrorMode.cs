namespace HexUN.Convention
{
    /// <summary>
    /// How should the folder structure of target be recreated in destination
    /// </summary>
    public enum EMirrorMode
    {
        /// <summary>
        /// The folder structure in destination after the root
        /// should match the folder structure in target
        /// </summary>
        Match = 0
    }
}
