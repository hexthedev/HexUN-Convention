using Hex.Paths;

namespace HexUN.Convention
{
    /// <summary>
    /// Takes a target yaml file and updates/creates an so file of the same
    /// name the the destination
    /// </summary>
    public class ConvertToSo : AFileOperation
    {
        public override void Operate(PathString target, PathString destination)
        {

        }
    }
}
